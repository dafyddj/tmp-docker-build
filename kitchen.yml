---
driver:
  name: docker

provisioner:
  name: dummy

verifier:
  name: shell

platforms:
  - name: test
    driver:
      image: dafyddj/tmp-docker-build:local-test
      platform: debian

suites:
  - name: default
